# Code Snapshot

这里保存第二课学习结束时的代码快照。

这些文件是从项目根目录复制出来的，用来归档当时的状态；后续课程继续修改项目根目录代码，不直接修改这里。

包含文件：

- `main.py`
- `config.py`
- `test_main.py`
- `pyproject.toml`

注意：`uv.lock` 没有复制到课程快照里，因为它是项目级锁文件，随整体依赖状态变化。
